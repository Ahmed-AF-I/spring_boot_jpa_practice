spring:
  application:
    name: Bouali

  datasource:
    url: jdbc:postgresql://localhost:5432/workshop
    driver-class-name: org.postgresql.Driver
    username: postgres
    password:

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    open-in-view: false